{
  "name": "aalgorix-project",
  "version": "1.0.0",
  "description": "Aalgorix World Academy — full-stack LMS (Next.js frontend + Express/Moodle backend)",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev":         "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev:frontend --prefix frontend\"",
    "dev:backend":  "npm run dev  --prefix backend",
    "dev:frontend": "npm run dev  --prefix frontend",
    "start":        "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm start --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
